import 'dart:ffi';

import 'package:ffi/ffi.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(const MyApp());
}
// golang -> add
//typedef NativeFunc = Int32 Function(Int32, Int32);
//typedef DarkFunc = int Function(int, int);

//golang -> gets
// typedef NativeFunc = Pointer<Utf8> Function();
// typedef DarkFunc = Pointer<Utf8> Function();

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    // golang -> add
    // final so = DynamicLibrary.open('./linux/libmymath.dll');
    // final fn = so.lookupFunction<NativeFunc, DarkFunc>('add');

    // golang -> gets
    // final gets = DynamicLibrary.open("./linux/libgets.dll");
    // final fns = gets.lookupFunction<NativeFunc, DarkFunc>('gets');

    return MaterialApp(
      title: 'Flutter FFI Demo',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: Scaffold(
        body: Center(
          child: Text(
            // fn(2, 3).toString(),//golang -> add
            fns().toDartString(), // golang -> gets
            style: const TextStyle(fontSize: 26),
          ),
        ),
      ),
      // home: const MyHomePage(title: 'Flutter Demo Home Page'),
    );
  }
}

class MyHomePage extends StatefulWidget {
  const MyHomePage({super.key, required this.title});
  final String title;

  @override
  State<MyHomePage> createState() => _MyHomePageState();
}

class _MyHomePageState extends State<MyHomePage> {
  int _counter = 0;

  void _incrementCounter() {
    setState(() {
      _counter++;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(widget.title),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            const Text(
              'You have pushed the button this many times:',
            ),
            Text(
              '$_counter',
              style: Theme.of(context).textTheme.headline4,
            ),
          ],
        ),
      ),
      floatingActionButton: FloatingActionButton(
        onPressed: _incrementCounter,
        tooltip: 'Increment',
        child: const Icon(Icons.add),
      ), // This trailing comma makes auto-formatting nicer for build methods.
    );
  }
}
